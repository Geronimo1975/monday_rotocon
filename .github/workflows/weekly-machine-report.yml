name: Weekly Machine Report

on:
  schedule:
    # GitHub cron is UTC with no DST. 08:00 Europe/Berlin is 06:00 UTC in
    # summer (CEST) and 07:00 UTC in winter (CET). We fire at both times; the
    # "gate" job below lets only the run that is actually 08:00 in Berlin proceed.
    - cron: "0 6 * * 1"
    - cron: "0 7 * * 1"
  workflow_dispatch: {}

jobs:
  gate:
    runs-on: ubuntu-latest
    outputs:
      proceed: ${{ steps.check.outputs.proceed }}
    steps:
      - id: check
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "Manual dispatch — proceeding."
            echo "proceed=true" >> "$GITHUB_OUTPUT"
          elif [ "$(TZ=Europe/Berlin date +%H)" = "08" ]; then
            echo "Berlin local time is 08:xx — proceeding."
            echo "proceed=true" >> "$GITHUB_OUTPUT"
          else
            echo "Berlin local hour is $(TZ=Europe/Berlin date +%H), not 08 — skipping this trigger."
            echo "proceed=false" >> "$GITHUB_OUTPUT"
          fi

  send-report:
    needs: gate
    if: needs.gate.outputs.proceed == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Install WeasyPrint system libraries
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf-2.0-0 libffi-dev

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"

      - name: Sync dependencies (with report extra)
        run: uv sync --extra report

      - name: Send weekly report
        env:
          MONDAY_API_TOKEN: ${{ secrets.MONDAY_API_TOKEN }}
          N8N_WEBHOOK_URL: ${{ secrets.N8N_WEBHOOK_URL }}
          N8N_WEBHOOK_TOKEN: ${{ secrets.N8N_WEBHOOK_TOKEN }}
          REPORT_RECIPIENT: ${{ secrets.REPORT_RECIPIENT }}
        run: uv run python scripts/weekly_machine_report.py
